[Unit]
Description=SWAutomorph Control Plan
After=network.target

[Service]
Type=forking
# GENERATED ARTIFACT - DO NOT rely on the literals below for a real install.
# The authoritative unit is emitted by 'aipoweredstore_cli.py install-as-systemctl-service',
# which substitutes User= and the /home/<user>/... paths from LINUX_USER_INSTALLATION in
# conf/deploy.ini. The values below use the psmc fallback default (matching that fallback) and
# the default PLTF_FOLDER; regenerate to get the correct install user and paths from deploy.ini.
ExecStart=/home/psmc/opcp-explorer/scripts/start_swautomorph_controlplan.sh
ExecStop=/home/psmc/opcp-explorer/scripts/stop_swautomorph_controlplan.sh
WorkingDirectory=/home/psmc/opcp-explorer
User=psmc

[Install]
WantedBy=multi-user.target
