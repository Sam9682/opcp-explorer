{
  "name": "opcp-explorer-js-tests",
  "version": "1.0.0",
  "private": true,
  "description": "jsdom-based test harness for the dashboard client-side JavaScript (sortable table headers feature).",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "fast-check": "^3.23.2",
    "jsdom": "^25.0.1"
  }
}
